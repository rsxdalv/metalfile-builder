package:
  name: hello
  version: 1.0
  architecture: all
  depends: [bash]
  description: Simple hello app for smoke testing metalfile

files:
  - src: ./hello.sh
    dest: /opt/hello/hello.sh
  - src: ./config.json
    dest: /etc/hello/config.json

conffiles:
  - /etc/hello/config.json

postinst: |
  echo "hello installed"
